function solution(num_list, n) {
    var answer = 0;
    if(num_list.includes(n)){
        answer =1;
    }
    return answer;
}

function solution(num_list, n) {
    return num_list.includes(n) ? 1 : 0;
}
